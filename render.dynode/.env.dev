# .env file
# Replace <username>, <password>, <cluster-url> with your actual MongoDB Atlas or local connection details
# For local MongoDB:
NODE_ENV=development
PORT_ENV=5555
JWT_SECRET=your_super_secret_key
SOURCE_API_URL=http://localhost:3333
BUILDER_BASE_URL=http://localhost:4444 # For browser requests
RENDER_BASE_URL=http://localhost:5555 # For browser requests
ECHO_BASE_URL=http://localhost:6666 # For browser requests
SERVICE_WORKERS=enabled # Enable if needed

# For MongoDB Atlas (example):
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/mydatabase?retryWrites=true&w=majority